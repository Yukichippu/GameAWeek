using UnityEngine;

public class CSVReader_LABYRINTH : MonoBehaviour
{
    [SerializeField]
    private string CSVFilePath;

    private string[] csvXLine;

    private void Update()
    {
        for(int x = 0; x < CSVFilePath.Length; x++)
        {
            for (int y = 0; y < CSVFilePath.Length; y++)
            {

            }
        }
    }
}
